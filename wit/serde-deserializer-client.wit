default world serde-deserializer-client {
    import deserializer: interface {
        use pkg.serde-de.{s128, unexpected, usize, u128}

        test: func(x: s128, y: usize) -> result<tuple<u128, usize>, unexpected>
    }
    
    export deserialize: interface {
        use pkg.serde-de.{s128, unexpected, usize, u128}

        test: func(x: s128, y: usize) -> result<tuple<u128, usize>, unexpected>
    }
}
